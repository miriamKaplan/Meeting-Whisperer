# 🔧 Jira Integration Setup for Cross River Bank

Your Meeting Whisperer is now configured to work with your Cross River Bank Jira board!

## 📋 Current Configuration
- **Jira URL**: https://crossriverbank.atlassian.net
- **Project**: BEKAMON
- **Board**: 492

## 🔑 To Enable Real Jira Integration

You need to get your Jira API credentials and update the `.env` file:

### Step 1: Get Your Jira API Token
1. Go to: https://id.atlassian.com/manage-profile/security/api-tokens
2. Click "Create API token"
3. Give it a name like "Meeting Whisperer"
4. Copy the generated token

### Step 2: Update Your .env File
Open `/Users/mkaplan/Desktop/Meeting-Whisperer/.env` and replace:

```
JIRA_EMAIL=your.email@crossriverbank.com
JIRA_API_TOKEN=your_jira_api_token_here
```

With your actual:
```
JIRA_EMAIL=firstname.lastname@crossriverbank.com
JIRA_API_TOKEN=ATATT3xFfGF0aJCJCqLwqI... (your token)
```

### Step 3: Restart Backend
After updating the .env file, restart the backend:
```bash
cd /Users/mkaplan/Desktop/Meeting-Whisperer/backend
pkill -f uvicorn
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

## ✅ Test Integration
1. Go to http://localhost:3002
2. Start recording and speak some action items
3. Click "📋 Add to Jira" on any action item
4. It should create a real ticket in your BEKAMON project!

## 🎯 Current Status
- **Demo Mode**: Currently active (will show popup but not create real tickets)
- **Real Mode**: Will activate once you add your credentials

The Jira integration will create tickets with:
- Smart summaries generated by Claude AI
- Proper assignees (if mentioned in conversation)
- Priority levels (high/medium/low)
- Rich descriptions with context

## 🆘 Need Help?
If you have issues:
1. Check your email has access to the BEKAMON project
2. Verify your API token is correct
3. Make sure you have permission to create tickets in BEKAMON